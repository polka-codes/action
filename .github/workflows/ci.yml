name: CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

permissions:
  contents: read
  pull-requests: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Install ripgrep
        run: |
          sudo apt-get update \
            && sudo apt-get install -y --no-install-recommends ripgrep

      - uses: actions/checkout@v4

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Run lint
        run: bun check

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Test Action source and shipped bundle
        run: bun test

# Generated by polka.codes
